---
export interface Props {
	name: string;
}

const { props } = Astro as { props: Props };
---
<sl-details class="breaking-change-details">
	<summary slot="summary">{props.name}</summary>
	<slot />
</sl-details>