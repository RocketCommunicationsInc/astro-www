---
import ProtectedLayout from '../../layouts/protected-layout.astro'
import PageHeader from '../../components/page-header/page-header.astro'
---

<ProtectedLayout
  title="Premium Components | Astro UXDS"
  description="Advanced UI components for sophisticated space applications."
  minRole="user"
>
  <main class="page protected-content" id="content">
    <PageHeader>
      <h1>Premium Components <span class="premium-badge">Premium</span></h1>
      <p>Advanced UI components for sophisticated space applications</p>
    </PageHeader>

    <div class="page-content">
      <div class="component-showcase">
        <section class="component-card">
          <h3>Advanced Data Grid</h3>
          <div class="component-preview">
            <div class="mock-data-grid">
              <div class="grid-header">
                <span>Mission</span>
                <span>Status</span>
                <span>Progress</span>
                <span>Actions</span>
              </div>
              <div class="grid-row">
                <span>Mars Rover</span>
                <span class="status-active">Active</span>
                <span class="progress-bar"><div style="width: 75%"></div></span>
                <span>View Details</span>
              </div>
              <div class="grid-row">
                <span>ISS Dock</span>
                <span class="status-pending">Pending</span>
                <span class="progress-bar"><div style="width: 45%"></div></span>
                <span>View Details</span>
              </div>
            </div>
          </div>
          <p>Sortable, filterable data grid with advanced features for mission data.</p>
          <a href="#" class="btn btn-outline">View Documentation</a>
        </section>

        <section class="component-card">
          <h3>Interactive Timeline</h3>
          <div class="component-preview">
            <div class="mock-timeline">
              <div class="timeline-item">
                <div class="timeline-marker timeline-marker--completed"></div>
                <div class="timeline-content">
                  <strong>Launch</strong>
                  <span>Mission commenced</span>
                </div>
              </div>
              <div class="timeline-item">
                <div class="timeline-marker timeline-marker--active"></div>
                <div class="timeline-content">
                  <strong>Transit</strong>
                  <span>En route to target</span>
                </div>
              </div>
              <div class="timeline-item">
                <div class="timeline-marker"></div>
                <div class="timeline-content">
                  <strong>Arrival</strong>
                  <span>Expected completion</span>
                </div>
              </div>
            </div>
          </div>
          <p>Visual timeline component for mission phases and milestones.</p>
          <a href="#" class="btn btn-outline">View Documentation</a>
        </section>

        <section class="component-card">
          <h3>System Monitor</h3>
          <div class="component-preview">
            <div class="mock-monitor">
              <div class="monitor-metric">
                <span class="metric-label">CPU</span>
                <div class="metric-gauge">
                  <div class="gauge-fill" style="width: 65%"></div>
                </div>
                <span class="metric-value">65%</span>
              </div>
              <div class="monitor-metric">
                <span class="metric-label">Memory</span>
                <div class="metric-gauge">
                  <div class="gauge-fill" style="width: 40%"></div>
                </div>
                <span class="metric-value">40%</span>
              </div>
              <div class="monitor-metric">
                <span class="metric-label">Power</span>
                <div class="metric-gauge">
                  <div class="gauge-fill" style="width: 90%"></div>
                </div>
                <span class="metric-value">90%</span>
              </div>
            </div>
          </div>
          <p>Real-time system monitoring with customizable thresholds and alerts.</p>
          <a href="#" class="btn btn-outline">View Documentation</a>
        </section>
      </div>
    </div>
  </main>
</ProtectedLayout>

<style>
  .component-showcase {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
    padding: 2rem;
  }

  .component-card {
    background: var(--color-background-surface, white);
    padding: 2rem;
    border-radius: var(--radius-large, 8px);
    border: 1px solid var(--color-border, #e5e7eb);
    box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
  }

  .component-card h3 {
    margin-bottom: 1rem;
    color: var(--color-text-primary, #111827);
  }

  .component-preview {
    margin-bottom: 1rem;
    padding: 1rem;
    background: var(--color-background, #f9fafb);
    border-radius: var(--radius-base, 6px);
    border: 1px solid var(--color-border, #e5e7eb);
  }

  /* Mock Data Grid */
  .mock-data-grid {
    font-size: 0.875rem;
  }

  .grid-header {
    display: grid;
    grid-template-columns: 1fr 80px 100px 80px;
    gap: 1rem;
    padding: 0.5rem 0;
    font-weight: 600;
    border-bottom: 1px solid var(--color-border, #e5e7eb);
    margin-bottom: 0.5rem;
  }

  .grid-row {
    display: grid;
    grid-template-columns: 1fr 80px 100px 80px;
    gap: 1rem;
    padding: 0.5rem 0;
    align-items: center;
  }

  .status-active {
    color: var(--color-success, #059669);
    font-weight: 600;
  }

  .status-pending {
    color: var(--color-warning, #d97706);
    font-weight: 600;
  }

  .progress-bar {
    background: var(--color-border, #e5e7eb);
    border-radius: 2px;
    height: 4px;
    overflow: hidden;
  }

  .progress-bar div {
    height: 100%;
    background: var(--color-primary, #0066cc);
    transition: width 0.3s ease;
  }

  /* Mock Timeline */
  .mock-timeline {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .timeline-item {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .timeline-marker {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--color-border, #e5e7eb);
    border: 2px solid var(--color-background-surface, white);
  }

  .timeline-marker--completed {
    background: var(--color-success, #059669);
  }

  .timeline-marker--active {
    background: var(--color-primary, #0066cc);
    animation: pulse 2s infinite;
  }

  .timeline-content {
    display: flex;
    flex-direction: column;
  }

  .timeline-content strong {
    font-weight: 600;
    color: var(--color-text-primary, #111827);
  }

  .timeline-content span {
    font-size: 0.875rem;
    color: var(--color-text-secondary, #6b7280);
  }

  /* Mock Monitor */
  .mock-monitor {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .monitor-metric {
    display: grid;
    grid-template-columns: 60px 1fr 40px;
    gap: 0.75rem;
    align-items: center;
    font-size: 0.875rem;
  }

  .metric-label {
    font-weight: 600;
    color: var(--color-text-primary, #111827);
  }

  .metric-gauge {
    background: var(--color-border, #e5e7eb);
    border-radius: 2px;
    height: 6px;
    overflow: hidden;
  }

  .gauge-fill {
    height: 100%;
    background: var(--color-primary, #0066cc);
    transition: width 0.3s ease;
  }

  .metric-value {
    text-align: right;
    font-weight: 600;
    color: var(--color-text-primary, #111827);
  }

  .btn {
    display: inline-block;
    padding: 0.5rem 1rem;
    text-decoration: none;
    border-radius: var(--radius-base, 6px);
    font-weight: 600;
    font-size: 0.875rem;
    transition: all 0.2s;
  }

  .btn-outline {
    background: transparent;
    color: var(--color-primary, #0066cc);
    border: 1px solid var(--color-primary, #0066cc);
  }

  .btn-outline:hover {
    background: var(--color-primary, #0066cc);
    color: white;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }
</style>